/* `common` is the shell; each game family owns one namespace, the PbtA hacks included in `pbta`. */
export const NAMESPACES = [
    'common',
    'legend',
    'city',
    'otherscape',
    'pbta',
    'adrenaline',
] as const
